import { View, Text } from "react-native";
import React from "react";

const ReplyTree = () => {
    return (
        <View
       className="w-[40px] h-[]"
      />
    );
  };

export default ReplyTree;
